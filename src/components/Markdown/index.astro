---
import {loadPropDefaults} from "../../loadPropDefaults.js"
import {render} from '@astro:content'
import "./style.scss"
const {text,variables} = await loadPropDefaults(import.meta.dirname + '/config.yaml',Astro.props)
---

<!--
<Content entry={text} />
-->
