---
import Map from "./Map.svelte"
import {loadPropDefaults} from "../../loadPropDefaults.js"
import schema from "./schema.yaml"
const props = await loadPropDefaults(schema,Astro.props)
---
<Map client:only="svelte" {...props} />
<!--
<p>{props.latitude}</p>
<p>{props.longitude}</p>
<p>{JSON.stringify(props)}</p>
<Map client:only="svelte" {...loadPropDefaults(import.meta.dirname + '/config.yaml',Astro.props)} />
-->
