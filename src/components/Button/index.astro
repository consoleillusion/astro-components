---
import {loadPropDefaults} from "../../loadPropDefaults.js"
import "./style.scss"
const {text,action} = await loadPropDefaults(import.meta.dirname + '/config.yaml',Astro.props)
---

<button {action}>{text}</button>
